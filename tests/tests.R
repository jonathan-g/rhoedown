library(rhoedown)

source(system.file('examples', 'markdownExtensions.R', package = 'rhoedown'), echo = TRUE)
source(system.file('examples', 'HTMLOptions.R', package = 'rhoedown'), echo = TRUE)

rm(mkd)
rm(tOpt)
